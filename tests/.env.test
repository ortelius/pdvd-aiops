# Test environment variables
# These are dummy values used during testing to prevent accidental API calls.
# Update these only if you need to run integration tests against real services.

ANTHROPIC_API_KEY=test-dummy-key-do-not-use
GITHUB_PERSONAL_ACCESS_TOKEN=test-dummy-token-do-not-use
